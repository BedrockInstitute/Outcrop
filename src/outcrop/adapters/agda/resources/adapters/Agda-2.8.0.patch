--- a/Agda.cabal
+++ b/Agda.cabal
@@ -953,0 +954 @@
+      Outcrop.Agda.TypeTrace
--- a/src/full/Agda/Main.hs
+++ b/src/full/Agda/Main.hs
@@ -37,0 +38 @@
+import Outcrop.Agda.TypeTrace (flushTypeTrace)
@@ -426,0 +428 @@
+  flushTypeTrace
--- a/src/full/Agda/Interaction/Imports.hs
+++ b/src/full/Agda/Interaction/Imports.hs
@@ -80,0 +81 @@
+import Outcrop.Agda.TypeTrace (flushPendingTypes)
@@ -1196 +1197,3 @@
-        Bench.billTo [Bench.Typing] $ mapM_ checkDeclCached ds `finally_` cacheCurrentLog
+        Bench.billTo [Bench.Typing] $
+          mapM_ (\ declaration -> checkDeclCached declaration >> flushPendingTypes) ds
+            `finally_` cacheCurrentLog
--- a/src/full/Agda/TypeChecking/Rules/Decl.hs
+++ b/src/full/Agda/TypeChecking/Rules/Decl.hs
@@ -64,0 +65 @@
+import qualified Outcrop.Agda.TypeTrace as Outcrop
@@ -139,0 +141 @@
+    Outcrop.traceDeclaration d
--- a/src/full/Agda/TypeChecking/Rules/Application.hs
+++ b/src/full/Agda/TypeChecking/Rules/Application.hs
@@ -38,0 +39 @@
+import qualified Outcrop.Agda.TypeTrace as Outcrop
@@ -301 +302 @@
-  case e of
+  result@(_, t) <- case e of
@@ -359,0 +361,6 @@
+  case e of
+    A.Var x
+      | A.nameBindingSite x == noRange -> Outcrop.traceType "name" e t
+      | otherwise -> Outcrop.traceType "binding" (A.nameBindingSite x) t
+    _ -> pure ()
+  pure result
@@ -806,0 +814,2 @@
+                let resultType = absApp b u
+                lift $ Outcrop.traceType "application" (fuseRange sFun e) resultType
@@ -808 +817 @@
-                  checkArgumentsE' s{ sFunType = absApp b u }
+                  checkArgumentsE' s{ sFunType = resultType }
--- a/src/full/Agda/TypeChecking/Rules/LHS.hs
+++ b/src/full/Agda/TypeChecking/Rules/LHS.hs
@@ -47,0 +48 @@
+import qualified Outcrop.Agda.TypeTrace as Outcrop
@@ -194,0 +196,4 @@
+    recordPatternApplication p a action = case p of
+      A.ConP{} -> Outcrop.traceCheckedType "pattern" p (unDom a) action
+      _        -> action
+
@@ -202 +207 @@
-    update eq@(ProblemEq p v a) = reduce v >>= constructorForm >>= \case
+    update eq@(ProblemEq p v a) = recordPatternApplication p a $ reduce v >>= constructorForm >>= \case
@@ -684 +689,4 @@
-          mapM_ noShadowingOfConstructors eqs
+          forM_ eqs $ \case
+            ProblemEq (A.VarP x) _ a -> Outcrop.traceType "binding" (A.unBind x) (unDom a)
+            _                        -> pure ()
+          mapM_ noShadowingOfConstructors eqs
--- a/src/full/Agda/TypeChecking/Rules/Term.hs
+++ b/src/full/Agda/TypeChecking/Rules/Term.hs
@@ -37,0 +38 @@
+import qualified Outcrop.Agda.TypeTrace as Outcrop
@@ -157,2 +158,4 @@
-      applyRelevanceToContext shapeIrrelevant $
-        sort . Univ u <$> checkLevel arg
+      applyRelevanceToContext shapeIrrelevant $ do
+        type_ <- sort . Univ u <$> checkLevel arg
+        Outcrop.traceType "application" e (sort $ getSort type_)
+        pure type_
@@ -345,0 +347 @@
+    mapM_ (\ xp -> Outcrop.traceType "binding" (A.unBind $ A.binderName $ namedArg xp) t) xps
@@ -521,0 +524 @@
+      mapM_ (\ xp -> Outcrop.traceType "binding" (A.unBind $ A.binderName $ namedArg xp) argsT) xps
@@ -581,0 +585,2 @@
+        mapM_ (\ xp -> Outcrop.traceType "binding"
+          (A.unBind $ A.binderName $ namedArg xp) argT) xps
@@ -1174,0 +1180,5 @@
+recordApplicationType :: A.Expr -> Type -> TCM a -> TCM a
+recordApplicationType e t action = case appView e of
+  Application _ (_ : _) -> Outcrop.traceCheckedType "application" e t action
+  _                     -> action
+
@@ -1183,0 +1194 @@
+  recordApplicationType e t $
@@ -1244,2 +1255,9 @@
-          | isNothing (nameOf $ unArg x) && isNothing (A.binderPattern $ namedArg x) ->
-              checkExpr' cmp (A.Lam i (domainFree (getArgInfo x) $ A.unBind <$> namedArg x) e0) t
+          | isNothing (nameOf $ unArg x) && isNothing (A.binderPattern $ namedArg x) -> do
+              result <- checkExpr' cmp
+                (A.Lam i (domainFree (getArgInfo x) $ A.unBind <$> namedArg x) e0) t
+              TelV tel _ <- telViewUpTo 1 t
+              case tel of
+                ExtendTel dom _ -> Outcrop.traceType "binding"
+                  (A.unBind $ A.binderName $ namedArg x) (unDom dom)
+                EmptyTel -> pure ()
+              pure result
@@ -1587 +1605,5 @@
-  inferApplication exh hd args e
+  result@(_, t) <- inferApplication exh hd args e
+  case appView e of
+    Application _ (_ : _) -> Outcrop.traceType "application" e t
+    _                     -> pure ()
+  pure result
@@ -1670,0 +1693 @@
+  Outcrop.traceType "binding" (A.unBind x) t
@@ -1675,0 +1699 @@
+  Outcrop.traceType "binding" (A.unBind x) t
@@ -1748,0 +1773 @@
+        mapM_ (uncurry $ Outcrop.traceType "binding") $ List.zip xs ts
